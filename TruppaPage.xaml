<Page x:Class="Theater.TruppaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Theater"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1050"
      Title="TruppaPage" Loaded="OnLoad">

    <Grid d:Background="#FF242740">
        <StackPanel>
            <TextBlock Text="Наша Труппа" Foreground="White" FontSize="54"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       Margin="20 10"/>
            <TextBlock Text="Познакомьтесь с талантливыми артистами, которые оживляют наши постановки." 
                       Foreground="LightGray" FontSize="28" TextWrapping="WrapWithOverflow"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       Margin="20 10" TextAlignment="Center"/>

            <ItemsControl x:Name="TruppaIC" ItemsSource="{Binding truppa}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="5" HorizontalAlignment="Stretch" Width="300" 
                                Background="#FF2A2F5A" Margin="10 10">
                            <StackPanel Orientation="Vertical">
                                <Image Width="200" Height="200" VerticalAlignment="Center"
       HorizontalAlignment="Center" Source="{Binding Image}">
                                    <Image.Clip>
                                        <RectangleGeometry RadiusX="100" RadiusY="100"
                           Rect="0,0,200,200"/>
                                    </Image.Clip>
                                </Image>
                                <TextBlock Text="{Binding fullName}" FontSize="14" Foreground="LightGray"
                                           Margin="10"/>
                                <TextBlock Text="{Binding birthDate}" FontSize="14" Foreground="LightGray"
                                           Margin="10"/>
                                <TextBlock Text="{Binding description}" FontSize="14" Foreground="LightGray"
                                           Margin="10" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </Grid>
</Page>
